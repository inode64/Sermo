kind: profile
name: catalina-sh-%v
display_name: "Apache Tomcat ${version}"
service: catalina-sh
variables:
  binary: "/usr/share/tomcat-${version}/bin/catalina.sh"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","version"], timeout: 10s }
