kind: profile
name: ssh
display_name: "OpenSSH"
service: { name: "ssh", backend: auto }
variables:
  binary: "/usr/bin/ssh"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","-V"], timeout: 10s }
  config: { type: command, command: ["${binary}","-t"], timeout: 30s }
