kind: profile
name: snmp-ups
display_name: "SNMP UPS Driver"
service: { name: "snmp-ups", backend: auto }
variables:
  binary: "/lib/nut/snmp-ups"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","-V"], timeout: 10s }
