kind: profile
name: asterisk
description: "Asterisk"
service: { name: "asterisk", backend: auto }
variables:
  binary: "/usr/sbin/asterisk"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["/usr/sbin/asterisk","-V"], timeout: 10s }
