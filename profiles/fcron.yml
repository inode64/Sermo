kind: profile
name: fcron
display_name: "Fcron"
description: "Fcron"
service: { name: "fcron", backend: auto }
variables:
  binary: "/usr/libexec/fcron"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","-V"], timeout: 10s }
