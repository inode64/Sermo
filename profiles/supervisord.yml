kind: profile
name: supervisord
display_name: "Supervisor"
service: { name: "supervisord", backend: auto }
variables:
  binary: "/usr/bin/supervisord"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","--version"], timeout: 10s }
