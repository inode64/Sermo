kind: profile
name: python%n
display_name: "Python ${n}"
service: { name: "python${n}", backend: auto }
variables:
  binary: "/usr/bin/python${n}"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","--version"], timeout: 10s }
