kind: profile
name: dbus-daemon
description: "DBus Daemon"
service: { name: "dbus-daemon", backend: auto }
variables:
  binary: "/usr/bin/dbus-daemon"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","--version"], timeout: 10s }
