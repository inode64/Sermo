kind: profile
name: upsmon
display_name: "NUT UPS Monitor"
description: "Upsmon"
service: { name: "upsmon", backend: auto }
variables:
  binary: "/usr/sbin/upsmon"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","-V"], timeout: 10s }
