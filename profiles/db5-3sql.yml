kind: profile
name: db5-3sql
description: "Oracle Berkeley DB"
service: { name: "db5-3sql", backend: auto }
variables:
  binary: "/usr/bin/db5.3sql"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["/usr/bin/db5.3sql","--version"], timeout: 10s }
