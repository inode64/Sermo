kind: profile
name: automount
display_name: "Automount"
description: "Automount"
service: { name: "automount", backend: auto }
variables:
  binary: "/usr/sbin/automount"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","-V"], timeout: 10s }
