kind: profile
name: rpc-statd
display_name: "NFS Status Monitor"
description: "RPC"
service: { name: "rpc-statd" }
variables:
  binary: "/sbin/rpc.statd"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","--version"], timeout: 10s }
