kind: profile
name: upsd
display_name: "NUT UPS Daemon"
description: "Upsd"
service: { name: "upsd", backend: auto }
variables:
  binary: "/usr/sbin/upsd"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","-V"], timeout: 10s }
