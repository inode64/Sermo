kind: profile
name: postgres-13
description: "PostgreSQL 13.0"
service: { name: "postgres", backend: auto }
variables:
  binary: "/usr/lib64/postgresql-13/bin/postgres"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["/usr/lib64/postgresql-13/bin/postgres","--version"], timeout: 10s }
