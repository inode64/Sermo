kind: profile
name: in-tftpd
display_name: "TFTP Server"
description: "In.tftpd"
service: { name: "in-tftpd", backend: auto }
variables:
  binary: "/usr/sbin/in.tftpd"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","-V"], timeout: 10s }
