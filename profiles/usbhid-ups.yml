kind: profile
name: usbhid-ups
display_name: "USB HID UPS Driver"
service: { name: "usbhid-ups" }
variables:
  binary: "/lib/nut/usbhid-ups"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","-V"], timeout: 10s }
