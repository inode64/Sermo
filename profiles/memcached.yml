kind: profile
name: memcached
display_name: "Memcached"
service: { name: "memcached", backend: auto }
variables:
  binary: "/usr/bin/memcached"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","-V"], timeout: 10s }
