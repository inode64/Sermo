kind: profile
name: postgres-9-3
description: "PostgreSQL 9.3"
service: { name: "postgres", backend: auto }
variables:
  binary: "/usr/lib64/postgresql-9.3/bin/postgres"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["/usr/lib64/postgresql-9.3/bin/postgres","--version"], timeout: 10s }
