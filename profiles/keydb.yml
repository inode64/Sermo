kind: profile
name: keydb-server
display_name: "KeyDB"
service: keydb-server
variables:
  port: 6379
  binary: "/usr/sbin/keydb-server"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","-v"], timeout: 10s }

checks:
  port: { type: tcp, port: "${port}" }
