kind: profile
name: java
description: "Java"
service: { name: "java", backend: auto }
variables:
  binary: "/usr/bin/java"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","-version"], timeout: 10s }
