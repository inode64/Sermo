kind: profile
name: xinetd
service: { name: "xinetd", backend: auto }
variables:
  binary: "/usr/sbin/xinetd"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","-version"], timeout: 10s }
