kind: profile
name: acpid
display_name: "ACPI Daemon"
service: { name: "acpid", backend: auto }
variables:
  binary: "/usr/sbin/acpid"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","-v"], timeout: 10s }
