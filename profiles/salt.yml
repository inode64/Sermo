kind: profile
name: salt
display_name: "Salt"
service: { name: "salt", backend: auto }
variables:
  binary: "/usr/bin/salt"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","--version"], timeout: 10s }
