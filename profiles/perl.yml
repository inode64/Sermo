kind: profile
name: perl
display_name: "Perl"
service: { name: "perl", backend: auto }
variables:
  binary: "/usr/bin/perl"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","-v"], timeout: 10s }
