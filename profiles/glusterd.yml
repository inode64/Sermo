kind: profile
name: glusterd
display_name: "GlusterFS"
description: "GlusterFS"
service: { name: "glusterd" }
variables:
  binary: "/usr/sbin/glusterd"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","--version"], timeout: 10s }
