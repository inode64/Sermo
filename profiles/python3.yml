kind: profile
name: python3
display_name: "Python 3"
description: "Python 3"
service: { name: "python3", backend: auto }
variables:
  binary: "/usr/bin/python3"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","--version"], timeout: 10s }
