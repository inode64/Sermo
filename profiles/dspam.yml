kind: profile
name: dspam
display_name: "DSPAM"
service: { name: "dspam", backend: auto }
variables:
  binary: "/usr/bin/dspam"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","--version"], timeout: 10s }
