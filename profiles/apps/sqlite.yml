kind: profile
name: sqlite
display_name: "SQLite"
service: { name: "sqlite", backend: auto }
variables:
  binary: "/usr/bin/sqlite3"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","-version"], timeout: 10s }
