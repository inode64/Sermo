kind: profile
name: qemu-system-x86-64
display_name: "QEMU"
description: "QEMU"
variables:
  binary: "/usr/bin/qemu-system-x86_64"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","--version"], timeout: 10s }
