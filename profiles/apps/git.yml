kind: profile
name: git
display_name: "Git"
service: { name: "git", backend: auto }
variables:
  binary: "/usr/bin/git"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","--version"], timeout: 10s }
