kind: profile
name: postgres-8-2
description: "PostgreSQL 8.2"
service: { name: "postgres", backend: auto }
variables:
  binary: "/usr/lib64/postgresql-8.2/bin/postgres"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","--version"], timeout: 10s }
