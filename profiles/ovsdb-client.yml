kind: profile
name: ovsdb-client
description: "OpenVsiwtch Database"
service: { name: "ovsdb-client", backend: auto }
variables:
  binary: "/usr/bin/ovsdb-client"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","--version"], timeout: 10s }
