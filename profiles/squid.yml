kind: profile
name: squid
description: "Squid"
service: { name: "squid", backend: auto }
variables:
  binary: "/usr/sbin/squid"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","-v"], timeout: 10s }
  config: { type: command, command: ["${binary}","-k","check"], timeout: 30s }
