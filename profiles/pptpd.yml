kind: profile
name: pptpd
display_name: "PPTP Server"
description: "Pptpd"
service: { name: "pptpd", backend: auto }
variables:
  binary: "/usr/sbin/pptpd"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","-v"], timeout: 10s }
