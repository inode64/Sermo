kind: profile
name: virtlockd
display_name: "libvirt Lock Daemon"
service: { name: "virtlockd", backend: auto }
variables:
  binary: "/usr/sbin/virtlockd"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","-V"], timeout: 10s }
