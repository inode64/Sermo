kind: profile
name: virtlockd
description: "Virtlockd"
service: { name: "virtlockd", backend: auto }
variables:
  binary: "/usr/sbin/virtlockd"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["/usr/sbin/virtlockd","-V"], timeout: 10s }
