kind: profile
name: pppd
description: "Pppd"
service: { name: "pppd", backend: auto }
variables:
  binary: "/usr/sbin/pppd"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","--version"], timeout: 10s }
