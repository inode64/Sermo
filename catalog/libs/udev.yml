name: udev
display_name: "libudev"
description: "udev device-management library (libudev); a change triggers a restart of dependent services"
variables:
  binary: "/usr/lib64/libudev.so.1"
preflight:
  file: { type: file, path: "${binary}" }
