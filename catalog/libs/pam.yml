kind: lib
name: pam
display_name: "Linux-PAM"
description: "Pluggable Authentication Modules; a change triggers a restart of dependent services"
variables:
  binary: "/lib64/libpam.so.0"
preflight:
  binary: { type: binary, path: "${binary}" }
