kind: lib
name: openssl
display_name: "OpenSSL libcrypto"
description: "OpenSSL cryptography/TLS library (libcrypto/libssl); a change triggers a restart of dependent services"
binary: "/usr/lib64/libcrypto.so.3"
