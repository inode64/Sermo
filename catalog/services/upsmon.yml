kind: daemon
name: upsmon
display_name: "NUT UPS Monitor"
category: hardware
service: upsmon
apps: [upsmon]

pidfile: /run/nut/upsmon.pid

processes:
  main:
    exe: "${upsmon_binary}"

checks:
  service: { type: service, expect: active }
