kind: daemon
name: ntpd
display_name: "NTP Daemon"
description: "Network Time Protocol Daemon"
service: ntpd
apps: [ntpd]

variables:
  port: 123

checks:
  service: { type: service, expect: active }
  port: { type: ntp, port: "${port}" }
