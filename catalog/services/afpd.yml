kind: daemon
name: afpd
display_name: "Netatalk AFP Daemon"
service: afpd
apps: [afpd]

variables:
  port: 548

checks:
  service: { type: service, expect: active }
  port: { type: tcp, port: "${port}" }
