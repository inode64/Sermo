name: afpd
display_name: Netatalk AFP Daemon
category: storage
service: afpd
apps:
  - afpd
variables:
  port: 548
watches:
  service:
    check:
      type: service
      expect: active
  port:
    check:
      type: tcp
      port: ${port}
