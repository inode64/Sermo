name: lvm2-monitor
display_name: LVM2 monitor
category: storage
description: LVM2 dmeventd monitoring activation service.
service:
  systemd:
    - lvm2-monitor
apps:
  - lvm
restart_on_change:
  apps:
    - lvm
processes: { }
watches:
  service:
    check:
      type: service
      expect: active
