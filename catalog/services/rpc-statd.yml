kind: daemon
name: rpc-statd
display_name: "NFS Status Monitor"
category: storage
description: "RPC"
service:
  systemd: [rpc-statd]
  openrc: [rpc.statd, rpc-statd]
apps: [rpc-statd]

checks:
  service: { type: service, expect: active }
