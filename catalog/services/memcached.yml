kind: daemon
name: memcached
display_name: "Memcached"
service: memcached
apps: [memcached]

variables:
  port: 11211

checks:
  service: { type: service, expect: active }
  port: { type: tcp, port: "${port}" }
