name: pptpd
display_name: PPTP Server
category: network
service: pptpd
apps:
  - pptpd
variables:
  port: 1723
watches:
  service:
    check:
      type: service
      expect: active
  port:
    check:
      type: tcp
      port: ${port}
