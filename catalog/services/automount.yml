kind: daemon
name: automount
display_name: Automount
category: system
service:
  systemd:
    - autofs
    - automount
  openrc:
    - autofs
    - automount
apps:
  - automount
checks:
  service:
    type: service
    expect: active
  autofs:
    type: autofs
