name: automount
display_name: Automount
category: system
service:
  systemd:
    - autofs
    - automount
  openrc:
    - autofs
    - automount
apps:
  - automount
restart_on_change:
  apps:
    - automount
watches:
  service:
    check:
      type: service
      expect: active
  autofs:
    check:
      type: autofs
