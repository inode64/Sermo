kind: daemon
name: tuned
display_name: "TuneD"
category: system
description: "Dynamic system tuning daemon."
service:
  systemd: [tuned]
  openrc: [tuned]
apps: [tuned]

pidfile:
  - /run/tuned/tuned.pid
  - /run/tuned.pid

checks:
  service: { type: service, expect: active }
