kind: daemon
name: sermod
display_name: "Sermo Daemon"
category: observability
description: "Sermo monitoring daemon."
service: sermod

pidfile: /run/sermod.pid

checks:
  service: { type: service, expect: active }
