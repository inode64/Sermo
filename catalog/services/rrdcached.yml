kind: daemon
name: rrdcached
display_name: "RRD Cached"
category: observability
description: "RRDTool data caching daemon."
service: rrdcached

pidfile: /run/rrdcached.pid

socket: /run/rrdcached.sock

checks:
  service: { type: service, expect: active }
