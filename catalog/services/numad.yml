kind: daemon
name: numad
display_name: NUMA Daemon
category: hardware
service: numad
apps:
- numad
checks:
  service:
    type: service
    expect: active
  process:
    type: process
    exe: ${numad_binary}
    user: root
    state: running
    requires:
    - service
postflight:
  process:
    type: process
    exe: ${numad_binary}
    user: root
    state: running
rules:
  alert-if-memory-high:
    type: alert
    if:
      metric:
        scope: service
        name: memory
        op: '>'
        value: 60%
    for:
      duration: 10m
    then:
      action: alert
      message: ${display_name} memory usage is high
