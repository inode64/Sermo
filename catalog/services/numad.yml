name: numad
display_name: NUMA Daemon
category: hardware
service: numad
apps:
  - numad
checks:
  service:
    type: service
    expect: active
  process:
    type: process
    exe: ${numad_binary}
    user: root
    state: running
    requires:
      - service
    verify: true
watches:
  alert-if-memory-high:
    check:
      type: metric
      scope: service
      name: memory
      op: '>'
      value: 60%
      optional: true
    for:
      duration: 10m
    then:
      action: alert
      message: ${display_name} memory usage is high
