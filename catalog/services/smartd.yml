kind: daemon
name: smartd
display_name: "S.M.A.R.T. Daemon"
category: storage
service: smartd
apps: [smartd]

pidfile: /run/smartd.pid

processes:
  main:
    exe: "${smartd_binary}"

checks:
  service: { type: service, expect: active }
