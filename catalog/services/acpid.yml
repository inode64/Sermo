name: acpid
display_name: ACPI Daemon
category: hardware
service: acpid
apps:
  - acpid
pidfile: /run/acpid.pid
processes:
  main:
    exe: ${acpid_binary}
    user: root
checks:
  service:
    type: service
    expect: active
  acpid:
    type: acpid
    timeout: 3s
