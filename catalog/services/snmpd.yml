kind: daemon
name: snmpd
display_name: "Net-SNMP Daemon"
service: snmpd
apps: [snmpd]

variables:
  port: 161

checks:
  service: { type: service, expect: active }
  port: { type: snmp, port: "${port}" }
