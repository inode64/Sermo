kind: daemon
name: dbus-daemon
display_name: "D-Bus"
description: "DBus Daemon"
service: dbus-daemon
apps: [dbus-daemon]

checks:
  service: { type: service, expect: active }
  dbus: { type: dbus, timeout: 3s }
