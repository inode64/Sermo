name: rpc-pipefs
display_name: RPC pipefs
category: storage
description: RPC pipefs mount helper used by NFS services.
service:
  systemd:
    - rpc-pipefs
  openrc:
    - rpc.pipefs
    - rpc-pipefs
processes: { }
checks:
  service:
    type: service
    expect: active
