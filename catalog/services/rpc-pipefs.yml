name: rpc-pipefs
display_name: RPC pipefs
category: storage
description: RPC pipefs mount helper used by NFS services.
service:
  systemd:
    - rpc-pipefs
  openrc:
    - rpc.pipefs
    - rpc-pipefs
processes: { }
watches:
  service:
    check:
      type: service
      expect: active
