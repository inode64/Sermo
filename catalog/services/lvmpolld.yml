name: lvmpolld
display_name: LVM poll daemon
category: storage
service: lvmpolld
apps:
  - lvmpolld
watches:
  service:
    check:
      type: service
      expect: active
  lvmpolld:
    check:
      type: lvmpolld
      timeout: 3s
