name: mdadm
category: storage
service: mdadm
apps:
  - mdadm
pidfile: /run/mdadm.pid
processes:
  main:
    exe: ${mdadm_binary}
    user: root
checks:
  service:
    type: service
    expect: active
  raid:
    type: raid
    degraded:
      op: '>'
      value: 0
