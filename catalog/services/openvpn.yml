kind: daemon
name: openvpn
display_name: "OpenVPN"
category: network
service: openvpn
apps: [openvpn]

variables:
  port: 1194

checks:
  service: { type: service, expect: active }
  port: { type: openvpn, port: "${port}" }
