kind: daemon
name: polkit
display_name: "Polkit"
category: system
description: "PolicyKit authorization service."
service:
  systemd: [polkit]
apps: [polkit]

processes:
  main:
    exe: "${polkit_binary}"

checks:
  service: { type: service, expect: active }
