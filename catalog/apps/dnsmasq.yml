kind: app
name: dnsmasq
display_name: "dnsmasq"
category: network
variables:
  binary: ${bindir}/dnsmasq
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}", "--version"], timeout: 10s }
