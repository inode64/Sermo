name: pcp
display_name: "Performance Co-Pilot"
category: observability
variables:
  binary: ${bindir}/pcp
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}", "-V"], timeout: 10s }
