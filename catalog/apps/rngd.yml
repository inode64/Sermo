kind: app
name: rngd
display_name: "RNG Daemon"
category: hardware
variables:
  binary: /usr/sbin/rngd
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}", "--version"], timeout: 10s }