name: colord
display_name: "colord"
category: hardware
variables:
  binary:
    - /usr/libexec/colord
    - /usr/lib/colord
preflight:
  binary: { type: binary, path: "${binary}" }
  health: { type: command, command: ["${binary}", "--help"], timeout: 10s }
