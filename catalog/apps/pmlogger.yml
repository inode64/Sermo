kind: app
name: pmlogger
display_name: "PCP Archive Logger"
category: observability
version_from: pcp
variables:
  binary: /usr/bin/pmlogger
preflight:
  binary: { type: binary, path: "${binary}" }
  health: { type: command, command: ["${binary}", "-?"], timeout: 10s, expect_exit: [0, 1] }
