kind: app
name: upsmon
display_name: "NUT UPS Monitor"
category: hardware
variables:
  binary: ${bindir}/upsmon
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","-V"], timeout: 10s }
