kind: app
name: bacula-dir
display_name: "Bacula Director"
category: storage
variables:
  binary: ${bindir}/bacula-dir
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}", "-?"], expect_exit: 1, timeout: 10s }
