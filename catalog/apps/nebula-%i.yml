kind: app
name: nebula-%i
display_name: "Nebula ${instance}"
category: network
versions:
  from: "/etc/nebula/${instance}.yml"
binary: /usr/bin/nebula
preflight:
  version: { type: command, command: ["${binary}", "-version"], timeout: 10s }
