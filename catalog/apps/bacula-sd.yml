kind: app
name: bacula-sd
display_name: "Bacula Storage Daemon"
category: storage
binary: "/usr/bin/bacula-sd"
preflight:
  version: { type: command, command: ["${binary}","-?"], expect_exit: 1, timeout: 10s }
