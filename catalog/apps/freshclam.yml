name: freshclam
display_name: "FreshClam"
category: security
description: "ClamAV virus database updater"
variables:
  binary: ${bindir}/freshclam
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}", "--version"], timeout: 10s }
