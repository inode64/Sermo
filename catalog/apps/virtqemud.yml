name: virtqemud
display_name: "libvirt QEMU Daemon"
category: virtualization
variables:
  binary: ${bindir}/virtqemud
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}", "-V"], timeout: 10s }
