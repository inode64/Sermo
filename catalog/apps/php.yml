kind: app
name: php
display_name: "PHP"
description: "PHP interpreter (active slot)"
variables:
  binary: "/usr/bin/php"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","-v"], timeout: 10s }
