kind: app
name: mysqld_exporter
display_name: "Prometheus MySQL Exporter"
category: observability
binary: /usr/bin/mysqld_exporter
preflight:
  version: { type: command, command: ["${binary}", "--version"], timeout: 10s }
