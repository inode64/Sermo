kind: app
name: varnishd
display_name: "Varnish"
category: web
variables:
  binary: ${bindir}/varnishd
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}", "-V"], timeout: 10s }
