name: snmp-ups
display_name: "SNMP UPS Driver"
category: hardware
variables:
  binary:
    - /usr/lib64/nut/snmp-ups
    - /lib64/nut/snmp-ups
    - /usr/lib/nut/snmp-ups
    - /lib/nut/snmp-ups
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}", "-V"], timeout: 10s }
