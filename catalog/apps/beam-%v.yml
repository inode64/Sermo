kind: app
name: beam-%v
display_name: "Erlang BEAM ${version}"
category: messaging
service: beam
binary: "/usr/lib64/erlang/erts-${version}/bin/beam"
preflight:
  version: { type: command, command: ["${binary}","-V"], timeout: 10s }
