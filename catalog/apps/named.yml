kind: app
name: named
display_name: "BIND (named)"
category: network
variables:
  binary: "/usr/bin/named"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","-v"], timeout: 10s }
