kind: app
name: ceph-mds
display_name: "Ceph Metadata Server"
category: storage
description: "Ceph CephFS metadata server daemon"
binary: "/usr/bin/ceph-mds"
preflight:
  version: { type: command, command: ["${binary}", "--version"], timeout: 10s }
