kind: app
name: avahi
display_name: "Avahi"
category: network
variables:
  binary: ${bindir}/avahi-daemon
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}", "-V"], timeout: 10s }
