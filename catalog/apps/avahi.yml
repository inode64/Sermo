kind: app
name: avahi
catalog_aliases: [avahi-daemon]
display_name: "Avahi"
category: network
binary: "/usr/bin/avahi-daemon"
preflight:
  version: { type: command, command: ["${binary}","-V"], timeout: 10s }
