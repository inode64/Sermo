kind: app
name: bareos-fd
display_name: "Bareos File Daemon"
category: storage
binary: "/usr/bin/bareos-fd"
preflight:
  version: { type: command, command: ["${binary}","-?"], expect_exit: 1, timeout: 10s }
