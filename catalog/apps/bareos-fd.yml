name: bareos-fd
display_name: "Bareos File Daemon"
category: storage
variables:
  binary: ${bindir}/bareos-fd
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}", "-?"], expect_exit: [0, 1], timeout: 10s }
