name: filebeat
display_name: "Filebeat"
category: observability
variables:
  binary: ${bindir}/filebeat
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}", "version"], timeout: 10s }
