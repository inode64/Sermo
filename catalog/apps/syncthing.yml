name: syncthing
display_name: "Syncthing"
category: storage
variables:
  binary: ${bindir}/syncthing
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}", "--version"], timeout: 10s }
