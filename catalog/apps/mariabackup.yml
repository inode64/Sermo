name: mariadb-backup
display_name: "MariaDB Backup"
category: database
variables:
  binary:
    - ${bindir}/mariadb-backup
    - ${bindir}/mariadbbackup
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}", "--version"], timeout: 10s }
