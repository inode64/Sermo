kind: app
name: rest-server
display_name: "Restic REST Server"
category: storage
variables:
  binary: /usr/bin/rest-server
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}", "--version"], timeout: 10s }
