kind: app
name: dmeventd
display_name: "Device-mapper event daemon"
category: system
description: "Monitors and auto-repairs DM devices (mirrors, snapshots, thin pools)"
binary: /usr/bin/dmeventd
preflight:
  version: { type: command, command: ["${binary}", "-V"], timeout: 10s }
