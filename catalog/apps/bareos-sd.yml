kind: app
name: bareos-sd
display_name: "Bareos Storage Daemon"
category: storage
binary: "/usr/bin/bareos-sd"
preflight:
  version: { type: command, command: ["${binary}","-?"], expect_exit: 1, timeout: 10s }
