name: bareos-sd
display_name: "Bareos Storage Daemon"
category: storage
variables:
  binary: ${bindir}/bareos-sd
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}", "-?"], expect_exit: [0, 1], timeout: 10s }
