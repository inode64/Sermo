kind: app
name: nebula
display_name: "Nebula"
category: network
variables:
  binary: ${bindir}/nebula
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}", "-version"], timeout: 10s }
