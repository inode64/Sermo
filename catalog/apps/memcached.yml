kind: app
name: memcached
display_name: "Memcached"
category: database
description: "Memcached in-memory cache"
variables:
  binary: ${bindir}/memcached
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}", "-V"], timeout: 10s }
