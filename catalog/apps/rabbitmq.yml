kind: app
name: rabbitmq
display_name: "RabbitMQ"
category: messaging
binary: /usr/bin/rabbitmq-server
variables:
  ctl: /usr/bin/rabbitmqctl
preflight:
  version: { type: command, command: ["${ctl}", "version"], timeout: 10s, optional: true }
