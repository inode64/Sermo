kind: app
name: rabbitmq
display_name: "RabbitMQ"
variables:
  binary: /usr/bin/rabbitmq-server
  ctl: /usr/bin/rabbitmqctl
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${ctl}", "version"], timeout: 10s }
