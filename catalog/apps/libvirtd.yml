kind: app
name: libvirtd
display_name: "libvirt"
variables:
  binary: "/usr/bin/libvirtd"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","-V"], timeout: 10s }
