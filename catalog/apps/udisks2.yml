name: udisks2
display_name: "UDisks2"
category: storage
variables:
  binary: /usr/libexec/udisks2/udisksd
preflight:
  binary: { type: binary, path: "${binary}" }
  health: { type: command, command: ["${binary}", "--help"], timeout: 10s }
