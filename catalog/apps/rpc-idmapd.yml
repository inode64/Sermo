name: rpc-idmapd
display_name: "NFS idmapd"
category: storage
version_from: rpc-mountd
variables:
  binary: ${bindir}/rpc.idmapd
preflight:
  binary: { type: binary, path: "${binary}" }
  health: { type: command, command: ["${binary}", "-h"], timeout: 10s }
