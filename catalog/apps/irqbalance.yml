kind: app
name: irqbalance
category: hardware
variables:
  binary: ${bindir}/irqbalance
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}", "--version"], expect_exit: 1, timeout: 10s }
