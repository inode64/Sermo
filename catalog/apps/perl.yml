kind: app
name: perl
display_name: "Perl"
category: runtime
binary: "/usr/bin/perl"
preflight:
  version: { type: command, command: ["${binary}","-v"], timeout: 10s }
  version_short: { type: command, command: ["${binary}","-e","printf '%vd', $^V"], timeout: 10s }
