kind: app
name: smartd
display_name: "S.M.A.R.T. Daemon"
category: storage
variables:
  binary: ${bindir}/smartd
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}", "--version"], timeout: 10s }
