kind: app
name: smartctl_exporter
display_name: "Prometheus smartctl Exporter"
category: observability
binary: /usr/bin/smartctl_exporter
preflight:
  version: { type: command, command: ["${binary}", "--version"], timeout: 10s }
