kind: app
name: smbd
display_name: "Samba SMB"
category: storage
binary: "/usr/bin/smbd"
variables:
  testparm: "/usr/bin/testparm"
preflight:
  testparm: { type: binary, path: "${testparm}" }
  version: { type: command, command: ["${binary}","--version"], timeout: 10s }
