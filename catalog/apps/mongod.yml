name: mongod
display_name: "MongoDB"
category: database
variables:
  binary: ${bindir}/mongod
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}", "--version"], timeout: 10s }
