name: nebula-agent
display_name: Nebula Agent
category: vpn
variables:
  binary: ${bindir}/nebula-agent
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}", "version"], timeout: 10s }
