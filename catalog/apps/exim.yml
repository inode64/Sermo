kind: app
name: exim
display_name: "Exim"
category: messaging
variables:
  binary: ${bindir}/exim
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","-bV"], timeout: 10s }
