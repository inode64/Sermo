kind: app
name: java
display_name: "Java"
category: runtime
variables:
  binary: "/usr/bin/java"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","-version"], timeout: 10s }
