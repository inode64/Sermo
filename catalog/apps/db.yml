kind: app
name: db%v
display_name: "Berkeley DB ${version}"
category: database
description: "Oracle Berkeley DB"
variables:
  binary: "/usr/bin/db${version}"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","--version"], timeout: 10s }
