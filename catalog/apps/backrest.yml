name: backrest
display_name: "Backrest"
category: storage
variables:
  binary: ${bindir}/backrest
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}", "--version"], timeout: 10s, optional: true }
  health: { type: command, command: ["${binary}", "-help"], timeout: 10s }
