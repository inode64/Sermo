kind: app
name: backrest
display_name: "Backrest"
category: storage
binary: /usr/bin/backrest
preflight:
  version: { type: command, command: ["${binary}", "--version"], timeout: 10s }
  health: { type: command, command: ["${binary}", "-help"], timeout: 10s, expect_exit: 0 }
