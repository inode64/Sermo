name: lxc-monitord
display_name: "LXC monitor daemon"
category: virtualization
description: "LXC container state monitor daemon"
version_from: lxc
variables:
  binary:
    - /usr/libexec/lxc/lxc-monitord
    - /usr/lib/lxc/lxc-monitord
preflight:
  binary: { type: binary, path: "${binary}" }
