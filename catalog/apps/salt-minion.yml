name: salt-minion
display_name: "Salt Minion"
category: system
variables:
  binary: ${bindir}/salt-minion
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}", "--version"], timeout: 10s }
