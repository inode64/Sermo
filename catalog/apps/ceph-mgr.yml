kind: app
name: ceph-mgr
display_name: "Ceph Manager"
category: storage
description: "Ceph manager daemon binary."
variables:
  binary: ${bindir}/ceph-mgr
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}", "--version"], timeout: 10s }
