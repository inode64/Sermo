kind: app
name: mail
display_name: "Mail"
category: messaging
binary: "/usr/bin/mail"
preflight:
  version: { type: command, command: ["${binary}","--version"], timeout: 10s }
  config: { type: command, command: ["${binary}","-v","-e"], timeout: 30s }
