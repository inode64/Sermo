kind: app
name: fail2ban
display_name: "Fail2ban"
category: security
variables:
  binary: "/usr/bin/fail2ban-server"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","--version"], timeout: 10s }
