kind: app
name: dcc
display_name: "Distributed Checksum Clearinghouse"
category: mail
variables:
  binary: ${bindir}/dccifd
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}", "-V"], timeout: 10s }
