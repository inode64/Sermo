kind: app
name: influxdb
display_name: "InfluxDB"
category: database
variables:
  binary: ${bindir}/influxd
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}", "version"], timeout: 10s }
