name: rasdaemon
display_name: "RAS daemon"
category: hardware
variables:
  binary: ${bindir}/rasdaemon
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}", "--version"], timeout: 10s }
