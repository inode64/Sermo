kind: app
name: ovs-vswitchd
display_name: "Open vSwitch"
category: virtualization
binary: "/usr/bin/ovs-vswitchd"
preflight:
  version: { type: command, command: ["${binary}","--version"], timeout: 10s }
