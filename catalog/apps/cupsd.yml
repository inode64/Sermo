kind: app
name: cupsd
display_name: "CUPS"
category: system
variables:
  binary: "/usr/bin/cupsd"
preflight:
  binary: { type: binary, path: "${binary}" }
  health: { type: command, command: ["${binary}", "-h"], timeout: 10s, expect_exit: 0 }
