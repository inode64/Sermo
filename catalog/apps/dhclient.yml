kind: app
name: dhclient
display_name: "DHCP Client"
category: network
variables:
  binary: ${bindir}/dhclient
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}", "--version"], timeout: 10s }
