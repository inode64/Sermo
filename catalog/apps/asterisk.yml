name: asterisk
display_name: "Asterisk PBX"
category: messaging
variables:
  binary: ${bindir}/asterisk
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}", "-V"], timeout: 10s }
