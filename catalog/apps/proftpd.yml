kind: app
name: proftpd
display_name: "ProFTPD"
category: network
variables:
  binary: ${bindir}/proftpd
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}", "--version"], timeout: 10s }
