kind: app
name: sqlite
display_name: "SQLite"
category: database
variables:
  binary: ${bindir}/sqlite3
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","-version"], timeout: 10s }
