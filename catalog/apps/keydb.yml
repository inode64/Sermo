kind: app
name: keydb
display_name: "KeyDB"
category: database
variables:
  binary: ${bindir}/keydb-server
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","-v"], timeout: 10s }
