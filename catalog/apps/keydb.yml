kind: app
name: keydb
catalog_aliases: [keydb-server]
display_name: "KeyDB"
category: database
binary: "/usr/bin/keydb-server"
preflight:
  version: { type: command, command: ["${binary}","-v"], timeout: 10s }
