name: systemd
display_name: "systemd"
category: system
description: "systemd system and service manager"
variables:
  binary: ${bindir}/systemctl
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}", "--version"], timeout: 10s }
