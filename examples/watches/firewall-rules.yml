name: firewall-rules
category: network
# Watch that nftables/iptables still has rules loaded (useful for firewall loaders).
monitor: disabled
interval: 30s
check: { type: firewall_rules, backend: auto, min_rules: 1 }
then:
  hook:
    command: [/usr/local/bin/sermo-firewall-alert.sh]
