name: mariadb-main
uses: mariadb
# Inherits the mariadb daemon's unit candidates (service: systemd/openrc),
# checks (service/tcp/ping) and remediation rules. Override variables here when
# the instance differs from the daemon defaults, e.g. a non-standard port.
variables:
  port: 3306
