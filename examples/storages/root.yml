name: storage-root
display_name: Root filesystem
category: storage
monitor: disabled
interval: 30s
check:
  type: storage
  path: /
  used_pct: { op: ">=", value: "90%" }
  mounted: true
for: { cycles: 3 }
then:
  hook:
    command: [/usr/local/bin/sermo-storage-alert.sh, /]
