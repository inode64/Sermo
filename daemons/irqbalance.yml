kind: daemon
name: irqbalance
service: irqbalance
variables:
  binary: "/usr/sbin/irqbalance"
preflight:
  binary: { type: binary, path: "${binary}" }
  version: { type: command, command: ["${binary}","--version"], expect_exit: 1, timeout: 10s }
